On Map View Page
    Zoom --> when I do two finger movements up it should zoom in and down it should zoom out, this is on the node mind map when my mouse is in that box
    When I press view supporting message give an option to go the the message in the chat view (in this case you should go to where the message is not the bottom of the page)
    When I press view supporting messages the icons and colors of each person should be the same as those in the chat view (ex. Jacks should be green)
    

Chat View Page
    When a message is sent go to the bottom of the page
    When chat view is first opened start at the bottom of the page
    No two people should have the same color icon, they should all be unique colors, but every single one that is by a specific person should have the same icon, so Jacks should always be green and no one elses should be the same color green